Wisconsin Benchmark Suite
-------------------------
1. Run make to build the suite as follows
$make
Note: CSQL requires JDK1.6 and javac path should be specified in PATH env variable

2. Change the following parameters in csql.conf file
MAX_SYS_DB_SIZE=10485760
MAX_DB_SIZE=104857600

3. Set CSQL environment variables using setupenv.ksh script under installation root directory.
$. ./setupenv.ksh

4. Populate the database 
$java JDBCPopulate

5.To run jdbc benchmark
$java JDBCBench

6.To run odbc benchmark
$./odbcBench
