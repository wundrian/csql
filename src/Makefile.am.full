
# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
SUBDIRS = base storage adapter gateway sqllog sqlnetwork sql cache network jdbc odbc tools server faulttest
